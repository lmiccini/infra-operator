apiVersion: network.openstack.org/v1beta1
kind: BGPConfiguration
metadata:
  labels:
    app.kubernetes.io/name: bgpconfiguration
    app.kubernetes.io/instance: bgpconfiguration-sample
    app.kubernetes.io/part-of: infra-operator
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/created-by: infra-operator
  name: bgpconfiguration-sample
spec:
  frrConfigurationNamespace: metallb-system
  # Optional: Specify router ASNs to advertise to (if empty, advertises to all routers)
  # routerASNs:
  #   - 64999
  #   - 65000
  # Optional: Specify neighbor addresses to advertise to (if empty, advertises to all neighbors)
  # neighborAddresses:
  #   - "10.10.10.10"
  #   - "10.10.11.10"
  # Optional: Filter which pods to advertise based on labels (if empty, advertises all pods with additional networks)
  # podSelector:
  #   matchLabels:
  #     service: keystone
  #   # Or use matchExpressions for more complex filtering:
  #   # matchExpressions:
  #   #   - key: service
  #   #     operator: In
  #   #     values: ["keystone", "nova"]
