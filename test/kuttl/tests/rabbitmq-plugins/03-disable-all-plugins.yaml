#
# Step 3: Remove all additional plugins
#
apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - script: |
      oc patch rabbitmq.rabbitmq.openstack.org rabbitmq-plugins -n $NAMESPACE --type merge -p '{"spec":{"config":{"additionalPlugins":[]}}}'
